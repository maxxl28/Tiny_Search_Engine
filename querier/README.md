# Querier

## Assumptions

- `pageDirectory` has files named 1, 2, 3, ..., without gaps.
- The content of files in `pageDirectory` follows the crawler format 
- The content of the index file follows the format told by the indexer (one word per line, followed by docID count pairs)

## Known Bugs

None.

## Functionality

To our knowledge, full