# Indexer

## Assumptions

- `pageDirectory` has files named 1, 2, 3, ..., without gaps.
- The content of files in `pageDirectory` follows the format defined in the specs (URL on first line, depth on second, HTML after).

## Known Bugs

None.

## Functionality

To our knowledge, full.